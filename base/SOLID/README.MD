1、开闭原则:
支持扩展，不支持修改

2、里氏替换原则
子类不应该修改父类原有的功能

3、依赖倒置原则
高模块不依赖底层模块,而是依赖抽象

4、单一职责原则
一个类只负责一个功能,避免修改一个地方而引发另外一个地方

5、接口隔离原则
不强制依赖不需要的方法或逻辑

6、迪米特法则
也就是所说的最少知道原则,只需要知道被依赖的类即可,被依赖的类中的复杂逻辑应该对其可见.
---只与朋友通信，不和陌生人说话

7、合成复用原则